#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
* _puts -prints a string to stdout
* @str: input string
*
* Return: Always 0
*/
void _puts(char *str)
{
	str = (char *)str;
	{
		puts(str);
		_putchar(str);
	{
	_putchar('\n');
}
