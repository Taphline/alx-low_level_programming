#include "main.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
* _strlen - get a string length
* @s: input string
*
* Return: integer len
*/
int _strlen(char *s)
{
	int a;

	s = (char *) s;
	a = strlen(s);

	return (a);
}
