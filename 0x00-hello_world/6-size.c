#include <stdio.h>

/**
 *main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf(Sizeof(char));
	printf(Sizeof(int));
	printf(Sizeof(long int));
	printf(Sizeof(long long int));
	printf(Sizeof(float));
	return (0);
}
